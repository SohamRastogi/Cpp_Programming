#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    cout<<pow(2,3)<<endl;;   // means 2 raised to the power 3.
    cout<<sqrt(20);          // means square root of 16.
    return 0 ;
}
// sqrt takes input in double as well as gives output in double unless typecasted.
/* int ans;
   ans = sqrt(20);

   in this case the output would be 4. */